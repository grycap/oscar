version: "2"
linters-settings:
  gocyclo:
    min-complexity: 15
  misspell:
    locale: US
  prealloc:
    skip-var-check: true

linters:
  disable-all: true
  enable:
    - errcheck
    - gocyclo
    - govet
    - ineffassign
    - staticcheck
    - unconvert
    - unused
    - misspell

run:
  timeout: 5m
  modules-download-mode: readonly
